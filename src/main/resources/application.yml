spring:
  application:
    name: test-app
  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: test
    password: test

  kafka:
    consumer:
      trusted_packages: 'm3.lib'
    bootstrap-servers: kafka:9092

alerter:
  telegram:
    token: teleToken
    chatId: teleChatId