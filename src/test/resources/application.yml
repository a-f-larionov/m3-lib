spring:
  datasource:
    url: jdbc:mysql://localhost:13306/junit_database
    username: junit_user
    password: junit_password

  kafka:
    topicName: 'test-topic'
    consumer:
      trusted_packages: 'm3.lib'

alerter:
  telegram:
    token: teleToken
    chatId: teleChatId